# Telegram WP Backup

Локальный Telegram-бот для резервного копирования и восстановления одного WordPress-сайта.

## Быстрый старт

```bash
uv sync
cp .env.example .env
# заполните .env реальными значениями
uv run python -m bot
```

Зависимости устанавливаются через `uv`.
